using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Score : MonoBehaviour
{
    public static int Score1 = 0;  //ストループ効果の得点
    public static int Score2 = 0;  //ノーマルモードの得点
    public static int mode   = 0;  //画面の切り替え用
    public static int mode2  = 0;  //センサーモードの切り替え用
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
